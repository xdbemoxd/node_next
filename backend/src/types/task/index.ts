export interface Task {
    urgency : number,
    name_task : string,
    description ?: string,
    id: number,
    due_date : Date
}

export interface Task_2 {
    urgency : number,
    name_task : string,
    description ?: string,
    due_date : Date
}